let gameState = "menu";

function setup() {
  createCanvas(1263, 617);
  background(220);

  // menuGrid.child(cnv);

  // let memoryText = createDiv('Memory Game');
  // memoryText.class('game-header');
  // memoryText.style('background')
  // memoryText.position(windowWidth/2.52, windowHeight/9);
  
  // let preview = createDiv();
  // preview.class("preview");
  // preview.position(250, 160);


  // let playButton = createButton('');
  // playButton.child('preview');
  // playButton.class('play-button');
  // playButton.mousePressed(statePlay);
}

function draw() {
}


